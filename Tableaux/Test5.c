#include <stdio.h>

  int T[10];
  int N,X;
  int i;
  
  int main(){
      printf("Remplir le tableaux STP :\n");
      for(int i=0 ;i< 10;i++){
       printf("remplisaage des elements:");
       scanf("%d",&T[i]);
      }
      printf("Entre une valeur N :");
      scanfg("%d",&N);
      for (int i=0; i < 10; i++){

      }

 return 0;
  }